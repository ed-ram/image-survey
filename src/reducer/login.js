import * as types from '../actions/types'


// initial state
const initialState = {
    loggedIn: false,
    token: null,
    id_token: null,
    checkingLogin: false
}

// the reducer
export function loginReducer(state = initialState, action) {

    }
}